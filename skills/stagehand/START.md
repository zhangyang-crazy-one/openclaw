# Stagehand 使用指南

## 📋 完整流程

### 1️⃣ 首次使用：手动登录所有网站

```bash
cd /home/liujerry/moltbot/skills/stagehand

source venv/bin/activate

python3 manual_login.py
```

**操作:**
- 浏览器会打开
- 手动登录 GitHub、Taobao 等所有需要的网站
- 按 **Ctrl+C** 保存 cookies

---

### 2️⃣ 之后使用：自动访问

#### 方式 A: 打开浏览器（手动操作）
```bash
python3 keep_open.py
```
- 自动加载登录状态
- 完全手动控制
- 按 Ctrl+C 保存

#### 方式 B: 执行自动化操作
```bash
python3 smart_search_with_screenshot.py
```
- 自动搜索 GitHub 仓库
- 带截图验证
- 自动保存 cookies

---

### 📁 文件说明

| 文件 | 用途 |
|------|------|
| `manual_login.py` | 手动登录（首次使用） |
| `keep_open.py` | 打开浏览器（长期使用） |
| `smart_search_with_screenshot.py` | 自动化操作演示 |

---

### 📂 目录

| 目录 | 用途 |
|------|------|
| `~/stagehand_screenshots/` | 截图保存位置 |
| `~/.stagehand/sessions/main/` | Cookies 保存位置 |

---

### 🔄 流程图

```
首次使用
    ↓
manual_login.py
    ↓
浏览器打开
    ↓
手动登录所有网站
    ↓
Ctrl+C 保存
    ↓
✅ 完成！

之后使用
    ↓
keep_open.py / smart_search.py
    ↓
自动加载 cookies
    ↓
✅ 已登录！
```

---

### 💡 提示

- Cookies 会长期保存
- 每次退出时会自动更新 cookies
- 截图保存在 `~/stagehand_screenshots/`
